
Types of tokens are:
			1. Identifiers - it is a identity of program - program name, method name
			2. Keywords - inbuilt/predefined/reserved words in python. there are 35 keywords in python
			3. Constants -  it is a fixed value stored in a variable 
			4. Variables - it is a name in which we can store single value at a time. variable is a container
			5. literals - 
			6. Operators


Typecast, type(x) 
eval -  eval() function parse the expression argument and evaluate it as a Python expression and runs Python expression (code) within the program.