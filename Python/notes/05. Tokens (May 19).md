4. Conditional Operators(If/else) : Used for conditions
5. Assignment Operator (=)
		NOTE:  == is a comparison operator while = is assignment operator
6. Ternary Operator: Single line condition when stored in a variable
		e.g. 
		
	```
	k = 'x is greater than y' if x>y else ' y is greater than x'
	print(k)
	print(k)
	print(k)
	print(k)
	```
	
7. Identity Operator = is , is not (address) Identity operator is an address of a variable. These operators are used to compare the addresses of variables. it is done by using "id" method
		
	```
		x=100
		y=200
		print(id(x))
		print(id(y))
		print( x is y)	 
	```

```
h= "hello there! general kenobi!"
x = input("enter the value:\n")
print ( x in h, h.count(x))

```
4. Membership Operator: in, not in
		membership operators are used to search any element in a given dataset
5. Binary/BIT-WISE Operator: <<(left shift), >>(Right Shift), &(Ampersand), | (pipeline),  ^(XOR), ~(not bit) 
	   (i).  Right Shift 
6. Special Operator: 
   ```
	   #,  \t, \n, end="",   \\
   ```
