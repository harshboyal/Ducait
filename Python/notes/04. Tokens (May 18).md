4. Conditional Operators(If/else) : Used for conditions
5. Assignment Operator (=)
		NOTE:  == is a comparison operator while = is assignment operator
6. Ternary Operator: Single line condition when stored in a variable
		e.g. 
		
	```
	k = 'x is greater than y' if x>y else ' y is greater than x'
	print(k)
	print(k)
	print(k)
	print(k)
	```
	
4. Identity Operator = is , is not (address) Identity operator is an address of a variable. These operators are used to compare the addresses of variables
		
	```
		 x=100
		 
	```
5. Membership Operator: in, not in
6. Binary/BIT-WISE Operator: <<(left shift), >>(Right Shift), &(Ampersand), | (pipeline),  ^(XOR), (not bit)
7. Special Operator: 
   ```
	   #,  \t, \n, end="",   \\
   ```
