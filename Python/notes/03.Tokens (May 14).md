5. Literals -  Literal is a value of a variable at the coding time.
6. Operators:  Operators are useful to perform logic operations, conditions in a code. Types of operators are:
	1. Arithmetical Operator : It is used to perform basic calculations+, - , * , / , / / , % , **
	2. Relational Operator:  It is also called comparison operator.It always returns a boolean datatype.  >, < , >=, <=, == , !=
	3. Logical Operator: and , or , not, 
	   and
		   T T T
		   T F F
		   F T F
		   F F F
		or
		  T T T
		  T F T
		  F T T
		  F F F
		not
		    The not keyword is a logical operator, and is used to reverse the result of the conditional statement.	   
	4. Conditional Operator : if , else
	5. Assignment Operator: =
	6. Ternary Operator: Single line condition
	7. Identity Operator = is , is not
	8. Membership Operator: in, not in
	9. Binary/BIT-WISE Operator: <<(left shift), >>(Right Shift), &(Ampersand), | (pipeline),  ^(XOR), ~(not bit)
	10. Special Operator:  #,  \t, \n, end="",   \\